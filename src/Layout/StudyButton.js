import React from "react";

const StudyButton = ({ onClick }) => {

    return (
        <button type="button" class="btn btn-primary" onClick={onClick}>Study</button>

    )
}

export default StudyButton